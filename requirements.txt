# Core
openai>=1.0.0
pydantic>=2.0.0

# RAG
# Pin to a modern Chroma that doesn't force numpy<2 (avoids building numpy from source on Python 3.14 Windows)
chromadb>=1.0.0
numpy>=2.0.0
sentence-transformers>=2.2.0

# Utils
python-dotenv>=1.0.0
rich>=13.0.0

# Notebook
jupyter>=1.0.0
ipywidgets>=8.0.0
